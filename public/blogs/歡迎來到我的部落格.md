<!-- 這是第一篇測試文章，用於測試搜尋系統、HTML標籤混合渲染以及標題呈現。這篇文章提到了Python、HTML、CSS以及GitHub Actions的整合。 -->

這是我部落格的第一篇文章！🎉

這篇文章主要用來展示 **Markdown** 語法的渲染效果，以及混合使用 HTML 標籤的能力。

## 標題測試

這是一個 `<h2>` 標題。

### H3 標題

這是一個 `<h3>` 標題。

## 混合 HTML 標籤

因為我們使用了支援 HTML 的 Markdown 解析器，所以我可以直接在文章裡寫 HTML：

<div style="background-color: rgba(255,255,255,0.1); padding: 15px; border-radius: 8px; border-left: 4px solid #00d2ff; margin-bottom: 20px;">
  <strong>注意：</strong> 這是一個使用原生 HTML <code>&lt;div&gt;</code> 寫出來的提醒區塊！你可以自由運用 CSS Inline Style！
</div>

## 程式碼區塊

這裡示範一段 Python 程式碼：

```python
def hello_world():
    print("歡迎來到 LongTai 的部落格！")
    
if __name__ == "__main__":
    hello_world()
```

## 搜尋測試

如果你在搜尋框輸入「測試」或是「Python」，應該就能透過文章最上方的隱藏註解找到這篇文章！
